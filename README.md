# AUTODATAGEN (MVP scaffold)
Agentic framework that auto-plans, implements & evaluates synthetic-data pipelines.

## Quickstart
```bash
git clone <repo>
cd autodatagen
conda env create -f environment.yml
conda activate autodatagen
